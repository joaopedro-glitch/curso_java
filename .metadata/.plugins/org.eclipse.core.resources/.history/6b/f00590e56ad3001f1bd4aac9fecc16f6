/*Leia 1 valor inteiro N, que representa o número de casos de teste que vem a seguir. Cada caso de teste consiste
de 3 valores reais, cada um deles com uma casa decimal. Apresente a média ponderada para cada um destes
conjuntos de 3 valores, sendo que o primeiro valor tem peso 2, o segundo valor tem peso 3 e o terceiro valor tem
peso 5.*/

package estruturas_de_repeticao;

import java.util.Scanner;

public class For_exc3 {

	public static void main(String[] args) {
		
		Scanner sc = new Scanner(System.in);
		
		int n, p1 = 2, p2 = 3, p3 = 5 ;
		double x, y, z, somaX = 0, somaY = 0, somaZ = 0;
		 
		System.out.printf("Insira o numero de casos de teste: ");
		n = sc.nextInt();
		
		for(int i = 0; i < n; i++) {

			System.out.printf("Atribua o primeiro valor real X (peso 2): ");
			x = sc.nextDouble();
			
			somaX += x;
			
			System.out.printf("Atribua o primeiro valor real Y (peso 3): ");
			y = sc.nextDouble();
			
			somaY += y;
			
			System.out.printf("Atribua o primeiro valor real Z (peso 5): ");
			z = sc.nextDouble();
			
			somaZ += z;
			
			double media = (somaX * p1) + (somaY * p2) + (somaZ * p3) / (p1 + p2 + p3);
			
			System.out.printf("A media ponderada dos tres valores eh: %.1f", media);
		}
				
		
		sc.close();

	}

}
